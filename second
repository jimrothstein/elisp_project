;; To run:   M-x ielm RET

(+ 4 5)

;; function
(defun add-nums (+ a b) )
(add-nums 2 3)

;; load, import library
(require 'ert)

;; return logical
(equal 5 5)


;; set-register to open  a file
(set-register ?i (cons 'file "~/code/elisp_project/second"))
