= ELISP Files Summary

#table(
  columns: (auto, 1fr),
  [*File*], [*Summary*],
  [010_basic_elisp.el], [Examples of elisp basics. Functions: my-insert-p-tag, add-nums. Shows symbols, setq, buffer functions.],
  [012_basic_fun.el], [Examples of elisp functions. Functions: multiply-by-seven, my_name. Covers let, lambda, eval, defvar.],
  [020_cdr_car.el], [Examples of list operations. Shows cons, cdr, car, setq, reverse on lists.],
  [030_eval.el], [Examples of evaluation. Shows setq, arithmetic (+), oddp predicate.],
  [040_operate_emacs.el], [Examples of Emacs operations. Shows frame parameters, emacs-version, set-register, predicates.],
  [050_display_buffer.el], [Buffer display configuration. Sets display-buffer-alist for shell and R buffers.],
  [110_basic_evil_config.el], [Evil mode configuration. Sets evil-normal-state-cursor, evil-insert-state-cursor, menu/tool bars.],
  [120_list_all_typst_related_files.el], [Function: my/typst-audit. Lists all Typst-related files in user-emacs-directory.],
  [130_purge_typst.el], [Function: my/typst-purge. Deletes all Typst-related packages and files with verification.],
  [510_protesilaos_fonts.el], [Examples from Prot's config. Shows set-face-attribute for fonts, symbol-value, message.],
  [my_first_transient.el], [Transient example. Defines my-first-transient prefix with hello/goodbye/list actions.],
  [simple_cdr_lambda_defun.el], [Examples of elisp basics. Covers cdr, car, lambda, defun, setq, set-register.],
  [tsc-hello.el], [Minimal transient example. Defines tsc-hello prefix with anonymous command suffix.],
)
